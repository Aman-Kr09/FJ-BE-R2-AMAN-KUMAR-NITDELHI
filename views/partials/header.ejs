<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %> | FinanceTracker
    </title>
    <link rel="stylesheet" href="/css/style.css">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="https://unpkg.com/lucide@latest"></script>
    <style>
        .currency-select {
            background: var(--bg-card);
            color: white;
            border: 1px solid var(--border);
            padding: 5px 10px;
            border-radius: 8px;
            font-size: 0.8rem;
            cursor: pointer;
            margin-right: 15px;
        }

        /* Hamburger/Dropdown Styles */
        .dropdown {
            position: relative;
            display: inline-block;
        }

        .dropdown-content {
            display: none;
            position: absolute;
            right: 0;
            background-color: var(--bg-card);
            min-width: 190px;
            box-shadow: 0px 8px 16px 0px rgba(0, 0, 0, 0.5);
            z-index: 1000;
            border-radius: 12px;
            border: 1px solid var(--border);
            margin-top: 10px;
            overflow: hidden;
            transform-origin: top right;
            animation: dropdownFade 0.2s cubic-bezier(0.4, 0, 0.2, 1);
        }

        @keyframes dropdownFade {
            from {
                opacity: 0;
                transform: scale(0.95);
            }

            to {
                opacity: 1;
                transform: scale(1);
            }
        }

        .dropdown-content.show {
            display: block;
        }

        .dropdown-content a {
            color: white;
            padding: 12px 16px;
            text-decoration: none;
            display: flex;
            align-items: center;
            gap: 12px;
            font-size: 0.9rem;
            transition: all 0.2s;
        }

        .dropdown-content a:hover {
            background-color: rgba(255, 255, 255, 0.05);
            color: var(--primary);
        }

        .menu-trigger {
            display: flex;
            align-items: center;
            gap: 8px;
            cursor: pointer;
            padding: 8px 15px;
            border-radius: 10px;
            transition: all 0.2s;
            border: 1px solid var(--border);
            background: var(--bg-card);
        }

        .menu-trigger:hover,
        .menu-trigger.active {
            background: rgba(255, 255, 255, 0.05);
            border-color: var(--primary);
        }

        @media (max-width: 768px) {
            .nav-links a:not(.logo) {
                display: none;
            }

            .menu-trigger span {
                display: none;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <nav style="display: flex; justify-content: space-between; align-items: center; padding: 20px 0;">
            <a href="<%= user ? '/dashboard' : '/' %>" class="logo">FinanceTracker</a>
            <div class="nav-links" style="display: flex; align-items: center; gap: 20px;">
                <% if (user) { %>
                    <form action="/user/update-currency" method="POST" style="display:inline;">
                        <select name="currency" class="currency-select" onchange="this.form.submit()">
                            <option value="USD" <%=user.currency==='USD' ? 'selected' : '' %>>USD $</option>
                            <option value="INR" <%=user.currency==='INR' ? 'selected' : '' %>>INR ₹</option>
                            <option value="EUR" <%=user.currency==='EUR' ? 'selected' : '' %>>EUR €</option>
                            <option value="GBP" <%=user.currency==='GBP' ? 'selected' : '' %>>GBP £</option>
                            <option value="JPY" <%=user.currency==='JPY' ? 'selected' : '' %>>JPY ¥</option>
                        </select>
                    </form>

                    <a href="/dashboard">Dashboard</a>
                    <a href="/transactions">Transactions</a>
                    <a href="/auth/ai" style="color: #a855f7; display: flex; align-items: center; gap: 5px;">
                        <i data-lucide="sparkles" style="width: 16px;"></i> AI Advisor
                    </a>

                    <!-- Hamburger Dropdown -->
                    <div class="dropdown">
                        <div class="menu-trigger">
                            <i data-lucide="menu" style="width: 22px;"></i>
                            <span>More</span>
                        </div>
                        <div class="dropdown-content">
                            <a href="/budgets"><i data-lucide="pie-chart" style="width: 18px;"></i> Budgets</a>
                            <a href="/savings"><i data-lucide="piggy-bank" style="width: 18px;"></i> Savings</a>
                            <a href="/reports"><i data-lucide="bar-chart-3" style="width: 18px;"></i> Reports</a>
                            <a href="/reports/anomalies"><i data-lucide="alert-triangle" style="width: 18px;"></i>
                                Anomalies</a>
                            <hr style="border: 0; border-top: 1px solid var(--border); margin: 5px 0;">
                            <a href="/user/profile"><i data-lucide="user-circle" style="width: 18px;"></i> My
                                Profile</a>
                            <a href="/auth/logout" style="color: var(--danger);">
                                <i data-lucide="log-out" style="width: 18px;"></i> Logout
                            </a>
                        </div>
                    </div>

                    <% } else { %>
                        <a href="/auth/login">Login</a>
                        <a href="/auth/register" class="btn-primary">Get Started</a>
                        <% } %>
            </div>
        </nav>

        <!-- Flash Messages -->
        <% if (locals.error && error.length> 0) { %>
            <div
                style="background: rgba(239, 68, 68, 0.1); border: 1px solid var(--danger); color: var(--danger); padding: 12px; border-radius: 12px; margin: 20px 0; text-align: center; display: flex; align-items: center; justify-content: center; gap: 10px;">
                <i data-lucide="alert-circle" style="width: 18px;"></i>
                <%= error %>
            </div>
            <% } %>

                <% if (locals.success && success.length> 0) { %>
                    <div
                        style="background: rgba(16, 185, 129, 0.1); border: 1px solid var(--success); color: var(--success); padding: 12px; border-radius: 12px; margin: 20px 0; text-align: center; display: flex; align-items: center; justify-content: center; gap: 10px;">
                        <i data-lucide="check-circle" style="width: 18px;"></i>
                        <%= success %>
                    </div>
                    <% } %>

                        <script>
                            // Initialize Lucide icons
                            lucide.createIcons();

                            // Hamburger Menu Logic
                            const menuTrigger = document.querySelector('.menu-trigger');
                            const dropdownContent = document.querySelector('.dropdown-content');

                            if (menuTrigger && dropdownContent) {
                                menuTrigger.addEventListener('click', (e) => {
                                    e.stopPropagation();
                                    dropdownContent.classList.toggle('show');
                                    menuTrigger.classList.toggle('active');
                                });

                                // Close menu when clicking elsewhere
                                document.addEventListener('click', (e) => {
                                    if (!dropdownContent.contains(e.target) && !menuTrigger.contains(e.target)) {
                                        dropdownContent.classList.remove('show');
                                        menuTrigger.classList.remove('active');
                                    }
                                });
                            }
                        </script>